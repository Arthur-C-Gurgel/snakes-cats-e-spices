class Prato {
    name: string;
    
    preco: number;

    descricao: string;


    constructor(name: string, preco: number, descricao: string,) {
        this.name = name

        this.preco = preco

        this.descricao = descricao
    }
}

export default Prato


